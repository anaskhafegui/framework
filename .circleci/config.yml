version: 2
jobs:
  build:
    branches:
      only:
        - master
    docker:
      - image: debian:stretch
    steps:
      - checkout
      - run: composer install -n --prefer-dist
      - run: ./vendor/bin/phpunit tests
      - store_test_results